# vim: tabstop=4 expandtab shiftwidth=4 softtabstop=4 ft=python
# Enable syntax completion

try:
    import readline
except ImportError:
    print "Readline not available"
else:
    import rlcompleter
    readline.parse_and_bind("tab: complete")
